CREATE TRIGGER trigger_AddAccounting
ON ORDER_LINES
FOR INSERT AS

INSERT INTO
ACCOUNTS (ACC_REVENUE, ACC_COST, ACC_PROFIT)
SELECT MENU_PRICE, MENU_COST, MENU_PRICE - MENU_COST
FROM MENU_OPTIONS M
INNER JOIN INSERTED I
	ON M.MENU_ID = I.MENU_ID